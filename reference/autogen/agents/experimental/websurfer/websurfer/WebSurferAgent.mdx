---
sidebarTitle: WebSurferAgent
title: autogen.agents.experimental.websurfer.websurfer.WebSurferAgent
---
<h2 id="autogen.agents.experimental.websurfer.websurfer.WebSurferAgent" class="doc doc-heading">
    <code class="doc-symbol doc-symbol-heading doc-symbol-class"></code>
    <span class="doc doc-object-name doc-class-name">WebSurferAgent</span>
</h2>

```python
WebSurferAgent(
    llm_config: dict[str, typing.Any],
    *args,
    web_tool: Literal['browser_use', 'crawl4ai'] = 'browser_use',
    web_tool_kwargs: dict[str, typing.Any] = None,
    **kwargs
)
```

    (In preview) A class for generic conversable agents which can be configured as assistant or user proxy.
    
    After receiving each message, the agent will send a reply to the sender unless the msg is a termination msg.
    For example, AssistantAgent and UserProxyAgent are subclasses of this class,
    configured with different default settings.
    
    To modify auto reply, override `generate_reply` method.
    To disable/enable human response in every turn, set `human_input_mode` to "NEVER" or "ALWAYS".
    To modify the way to get human input, override `get_human_input` method.
    To modify the way to execute code blocks, single code block, or function call, override `execute_code_blocks`,
    `run_code`, and `execute_function` methods respectively.

<b>Parameters:</b>
| Name | Description |
|--|--|
| `llm_config` | llm inference configuration.<br/><br/>Please refer to [OpenAIWrapper.create](/reference/autogen/OpenAIWrapper#create) for available options.<br/><br/>When using OpenAI or Azure OpenAI endpoints, please specify a non-empty 'model' either in `llm_config` or in each config of 'config_list' in `llm_config`.<br/><br/>To disable llm-based auto reply, set to False.<br/><br/>When set to None, will use self.DEFAULT_CONFIG, which defaults to False.<br/><br/>**Type:** `dict[str, typing.Any]` |
| `*args` |  |
| `web_tool` | **Type:** `Literal['browser_use', 'crawl4ai']`<br/><br/>**Default:** 'browser_use' |
| `web_tool_kwargs` | **Type:** `dict[str, typing.Any]`<br/><br/>**Default:** None |
| `**kwargs` |  |

### Instance Attributes

<code class="doc-symbol doc-symbol-heading doc-symbol-attribute"></code>
#### tools
<br />

    <br />