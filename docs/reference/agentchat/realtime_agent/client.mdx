---
sidebarTitle: client
title: agentchat.realtime_agent.client
---

## OpenAIRealtimeClient

```python
class OpenAIRealtimeClient()
```

(Experimental) Client for OpenAI Realtime API.

### \_\_init\_\_

```python
def __init__(agent, audio_adapter, function_observer: FunctionObserver)
```

(Experimental) Client for OpenAI Realtime API.

args:
agent: Agent instance
the agent to be used for the conversation
audio_adapter: RealtimeObserver
adapter for streaming the audio from the client
function_observer: FunctionObserver
observer for handling function calls

### register

```python
def register(observer)
```

Register an observer to the client.

### notify\_observers

```python
async def notify_observers(message)
```

Notify all observers of a message from the OpenAI Realtime API.

### function\_result

```python
async def function_result(call_id, result)
```

Send the result of a function call to the OpenAI Realtime API.

### send\_text

```python
async def send_text(*, role: str, text: str)
```

Send a text message to the OpenAI Realtime API.

### initialize\_session

```python
async def initialize_session()
```

Control initial session with OpenAI.

### session\_update

```python
async def session_update(session_options)
```

Send a session update to the OpenAI Realtime API.

### run

```python
async def run()
```

Run the client.

