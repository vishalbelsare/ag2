---
sidebarTitle: math_utils
title: autogen.math_utils
---

## Functions

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### eval_math_responses
<a href="#autogen.math_utils..eval_math_responses" class="headerlink" title="Permanent link"></a>

```python
eval_math_responses(
    responses,
    solution=None,
    **args
) -> 
```

    Select a response for a math problem using voting, and check if the response is correct if the solution is provided.

<b>Parameters:</b>
| Name | Description |
|--|--|
| `responses` | The list of responses.<br/><br/> |
| `solution=None` |  |
| `**args` |  |

<br />

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### get_answer
<a href="#autogen.math_utils..get_answer" class="headerlink" title="Permanent link"></a>

```python
get_answer(solution: str | None) -> str | None
```

    

<b>Parameters:</b>
| Name | Description |
|--|--|
| `solution` | **Type:** `str \| None` |

<br />

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### is_equiv
<a href="#autogen.math_utils..is_equiv" class="headerlink" title="Permanent link"></a>

```python
is_equiv(str1: str | None, str2: str | None) -> float
```

    Returns (as a float) whether two strings containing math are equivalent up to differences of formatting in
    - units
    - fractions
    - square roots
    - superfluous LaTeX.
    Source: https://github.com/hendrycks/math

<b>Parameters:</b>
| Name | Description |
|--|--|
| `str1` | **Type:** `str \| None` |
| `str2` | **Type:** `str \| None` |

<br />

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### is_equiv_chain_of_thought
<a href="#autogen.math_utils..is_equiv_chain_of_thought" class="headerlink" title="Permanent link"></a>

```python
is_equiv_chain_of_thought(str1: str, str2: str) -> float
```

    Strips the solution first before calling `is_equiv`.

<b>Parameters:</b>
| Name | Description |
|--|--|
| `str1` | **Type:** `str` |
| `str2` | **Type:** `str` |

<br />

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### last_boxed_only_string
<a href="#autogen.math_utils..last_boxed_only_string" class="headerlink" title="Permanent link"></a>

```python
last_boxed_only_string(string: str) -> str | None
```

    Source: https://github.com/hendrycks/math
    Extract the last \boxed`\{...}` or \fbox`\{...}` element from a string.

<b>Parameters:</b>
| Name | Description |
|--|--|
| `string` | **Type:** `str` |

<br />

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### remove_boxed
<a href="#autogen.math_utils..remove_boxed" class="headerlink" title="Permanent link"></a>

```python
remove_boxed(string: str) -> str | None
```

    Source: https://github.com/hendrycks/math
    Extract the text within a \boxed`\{...}` environment.
    
    Example:
    ```python
    > remove_boxed("\boxed\{\frac\{2}\{3}}")
    
    \frac\{2}\{3}
    ```

<b>Parameters:</b>
| Name | Description |
|--|--|
| `string` | **Type:** `str` |

<br />

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### solve_problem
<a href="#autogen.math_utils..solve_problem" class="headerlink" title="Permanent link"></a>

```python
solve_problem(problem: str, **config) -> str
```

    `(openai&lt;1)` Solve the math problem.

<b>Parameters:</b>
| Name | Description |
|--|--|
| `problem` | The problem statement.<br/><br/>**Type:** `str` |
| `**config` |  |

<b>Returns:</b>
| Type | Description |
|--|--|
| `str` | str: The solution to the problem. |

<br />

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### voting_counts
<a href="#autogen.math_utils..voting_counts" class="headerlink" title="Permanent link"></a>

```python
voting_counts(responses) -> 
```

    

<b>Parameters:</b>
| Name | Description |
|--|--|
| `responses` |  |

<br />