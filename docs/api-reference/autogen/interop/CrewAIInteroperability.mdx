---
sidebarTitle: CrewAIInteroperability
title: autogen.interop.CrewAIInteroperability
---
<h2 id="autogen.interop.CrewAIInteroperability" class="doc doc-heading">
    <code class="doc-symbol doc-symbol-heading doc-symbol-class"></code>
    <span class="doc doc-object-name doc-class-name">CrewAIInteroperability</span>
</h2>

```python
CrewAIInteroperability()
```

    A class implementing the `Interoperable` protocol for converting CrewAI tools
    to a general `Tool` format.
    
    This class takes a `CrewAITool` and converts it into a standard `Tool` object.

### Static Methods

<code class="doc-symbol doc-symbol-heading doc-symbol-method"></code>
#### convert_tool

```python
convert_tool(tool: Any, **kwargs: Any) -> autogen.tools.Tool
```

    Converts a given CrewAI tool into a general `Tool` format.
    
    This method ensures that the provided tool is a valid `CrewAITool`, sanitizes
    the tool's name, processes its description, and prepares a function to interact
    with the tool's arguments. It then returns a standardized `Tool` object.

<b>Parameters:</b>
| Name | Description |
|--|--|
| `tool` | The tool to convert, expected to be an instance of `CrewAITool`.<br/><br/>**Type:** `Any` |
| `**kwargs` | Additional arguments, which are not supported by this method.<br/><br/>**Type:** `Any` |

<b>Returns:</b>
| Type | Description |
|--|--|
| `autogen.tools.Tool` | Tool: A standardized `Tool` object converted from the CrewAI tool. |

<br />

<code class="doc-symbol doc-symbol-heading doc-symbol-method"></code>
#### get_unsupported_reason

```python
get_unsupported_reason() -> str | None
```

    

<br />